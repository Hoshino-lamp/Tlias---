<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.ClazzMapper">
    <select id="list" resultType="com.itheima.pojo.Clazz">
        select e.*,d.name masterName  from  class e left join emp d on e.master_id = d.id
            <where>
                <if test="name!=null and name!=''">
                    e.name=#{name}
                </if>
                <if test="end!=null and end!=''">
                    and e.end_date between #{begin} and #{end}
                </if>
            </where>
        order by e.update_time desc
    </select>
    <update id="update">
        update class
        <set>
            <if test="name != null and name != ''">name=#{name},</if>
            <if test="room != null and room != ''">room=#{room},</if>
            <if test="beginDate != null and beginDate != ''">begin_date=#{beginDate},</if>
            <if test="endDate != null and endDate != ''">end_date=#{endDate},</if>
            <if test="masterId != null and masterId=''" >master_id=#{masterId},</if>
            <if test="status != null and status != ''">status=#{status},</if>
            <if test="subject != null and suject=''">subject=#{subject},</if>
            <if test="updateTime != null">update_time=#{updateTime},</if>
        </set>
        where id=#{id}
    </update>
</mapper>